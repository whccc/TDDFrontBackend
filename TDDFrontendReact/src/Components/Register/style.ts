import styled from 'styled-components';

export const Container = styled.div`
  & input {
    border-top: none;
    border-left: none;
    border-right: none;
    border-radius: 0px;
  }
  & button {
    background-color: var(--bg-primary);
    width: 100%;
    border: none;
  }
`;

export const ContainerDataRegister = styled.div``;
